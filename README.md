# aspq2qasp
Script to translate ASP(Q) programs into QASP ones. Compatible with the PyQASP and qasp2qbf solvers.

# Usage
    $ aspq2qasp <file>
Where `<file>` is a .aspq file. 

If a file name is not specified, the program reads from stdin. 
